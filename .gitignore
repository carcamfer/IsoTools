# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Dashboard compilado: se genera en el build (Docker o `npm run web:build`).
# El lockfile de web/ SI se versiona: es el que hace reproducible ese build.
web/dist/

# Entornos (nunca commitear API keys / credenciales)
.env
.env.*
!.env*.example

# OS
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.swp
*~

# Obsidian — el vault del cerebro se versiona, pero el estado local del editor no
cerebro/.obsidian/workspace.json
cerebro/.obsidian/workspace-mobile.json
cerebro/.trash/
