# `node_modules` a cualquier profundidad: sin `**/`, Docker solo excluiria el de la
# raiz y copiaria el de `web/` dentro de la imagen — cientos de MB inutiles, con
# binarios compilados para el host equivocado.
**/node_modules
web/dist
.env
.env.local
.git
npm-debug.log*
docker-compose.yml
